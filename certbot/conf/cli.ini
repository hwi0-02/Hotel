# Certbot non-interactive configuration.
# Update the email and domains before running certbot.

authenticator = webroot
webroot-path = /var/www/certbot

rsa-key-size = 4096
text = True
agree-tos = True
non-interactive = True
preferred-chain = ISRG Root X1

# Replace with a valid email address used for Let's Encrypt notifications.
email = spvkf9030@gmail.com

# Comma-separated list of domains (e.g., example.com,www.example.com).
domains = hwiyeong.shop
